<h1 style="text-align:center;">All Projects</h1>

<% @project.each do |p| %>
	<div class="thumbnail col-lg-4 text-center">
		<h4><u><%= p.name %></u></h4>
		<button type="button"><%= link_to 'Show Details', "/projects/#{p.id}" %></button>
	</div>
<% end %>