
<div class="text-center">
	<h1>Details Page</h1>
	<h2><%= @project.name %></h2>
	<%= image_tag @project.image.url(:med) %>
	<p>Funding Amount: <%= @project.funding_goal %></p>
	<p><%= @project.description %></p>
	<p><%= render @project.rewards %></p>
</div>